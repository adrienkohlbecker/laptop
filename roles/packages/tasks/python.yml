---

- name: python | Install pip
  easy_install:
    name: pip
  tags:
    - packages
    - python

- name: python | Install ansible
  pip:
    name: ansible
  tags:
    - packages
    - python

- name: python | Install boto
  pip:
    name: boto
  become: true
  tags:
    - packages
    - python

- name: python | Install pylama
  pip:
    name: pylama
  become: true
  tags:
    - packages
    - python
